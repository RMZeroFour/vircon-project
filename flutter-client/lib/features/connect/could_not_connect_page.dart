import 'package:flutter/material.dart';

class CouldNotConnectPage extends StatelessWidget {
  const CouldNotConnectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Could Not Connect Page'),
      ),
    );
  }
}
